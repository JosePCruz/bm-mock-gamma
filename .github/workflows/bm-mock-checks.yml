name: mock gamma (fail)
on:
  pull_request:
    branches: [main]
  push:
    branches: [mock/bm-e2e]
jobs:
  check:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Intentional failure (mock)
        run: exit 1
